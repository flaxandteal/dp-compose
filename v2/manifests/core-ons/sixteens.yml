version: "3"
services:
  sixteens:
    build:
      context: ../../../../sixteens
      dockerfile: Dockerfile.local
    expose:
      - "9000"
    ports:
      - 9000:9000
    volumes:
      - ../../../../sixteens:/sixteens
    restart: unless-stopped
